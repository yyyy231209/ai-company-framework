# 入职消息模板（≤80 字）

> 给新员工的入职消息，控制 ≤80 字，一次并行发给所有新员工。

```
入职：读骨架 <companyRoot>\.dsh\skills\<岗位>.md，按 company-role-template 自写完整技能到同文件（职责/工具纪律/验收标准/汇报格式），再读经验库 <dshHome>\company-wisdom\<岗位>.md（无则跳过）。完成后回「已就位+技能已写」。只准在 companyRoot 内读写。
```
